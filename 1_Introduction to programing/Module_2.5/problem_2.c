// In this problem you will be given an integer number N. You will have to add 5 with N and print it.
#include <stdio.h>
int main()
{
    int N;
    scanf("%d", &N);
    int max = N + 5;
    printf("%d", max);
    return 0;
}